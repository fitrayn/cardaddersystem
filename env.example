# Server Configuration
NODE_ENV=development
PORT=4000

# MongoDB Atlas Configuration
MONGODB_URI=mongodb+srv://fitraynapp:mwp0jfjWFapPFOUr@cluster0.xrmj2mp.mongodb.net/card-adder?retryWrites=true&w=majority&appName=Cluster0

# Redis Configuration (Local)
REDIS_URL=redis://localhost:6379

# Security Configuration
JWT_SECRET=card-adder-system-super-secret-jwt-key-2024
AES_KEY_BASE64=dGVzdC1rZXktZm9yLWVuY3J5cHRpb24tdGVzdC1rZXk=
AES_IV_BASE64=dGVzdC1pdi1rZXk=

# CORS Configuration
CORS_ORIGIN=*

# Web Application
NEXT_PUBLIC_API_URL=http://localhost:4000

# Facebook GraphQL Doc IDs (override as needed)
# Add-Card mutation doc id
FB_DOC_ID=
# Encryption key mutation doc id (PaymentsCometGetServerEncryptionKeyMutation)
FB_ENC_KEY_DOC_ID=

# E2EE Controls
# Fail early when no public key is returned
ENFORCE_E2EE=false
# Emit diagnostic logs when the encryption key shape cannot be parsed
DEBUG_E2EE=false 